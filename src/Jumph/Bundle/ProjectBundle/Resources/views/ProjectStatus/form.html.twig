{% extends '::base.html.twig' %}

{% block title %}
    {{ parent() }} | Config | Projects
{% endblock %}

{% block headerContent %}
    <h1>
        Config | Projects
        <small>Add / Edit</small>
    </h1>
    <ol class="breadcrumb">
        <li><i class="fa fa-cogs"></i> Config</li>
        <li><a href="{{ path('jumph_config_project_status_overview') }}"><i class="fa fa-tasks"></i> Project status</a></li>
        <li> Add / Edit</li>
    </ol>
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-lg-8 col-md-12">
            <div class="box box-primary">
                {{ form_start(projectStatusForm, { 'attr': { 'id': 'main-form'}}) }}
                <div class="box-header">
                    <!-- Potential title -->
                </div>
                <div class="box-body">
                    {{ form_row(projectStatusForm.name) }}
                    {{ form_row(projectStatusForm.description) }}
                    {{ form_widget(projectStatusForm._token) }}
                </div>
                <div class="box-footer">
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
                {{ form_end(projectStatusForm, {'render_rest': false}) }}
            </div>
        </div>

        <div class="col-lg-4 col-md-12">
            <div class="box box-primary">
                <div class="box-header">
                    <!-- Potential title -->
                </div>
                <div class="box-body">
                    <!-- To be defined -->
                </div>
                <div class="box-footer">
                    <!-- Potential footer -->
                </div>
            </div>
        </div>
    </div>
{% endblock %}
