<li>
    <i class="fa fa-tasks bg-aqua"></i>
    <div class="timeline-item">
        <span class="time"><i class="fa fa-clock-o"></i> {{ stream.createdAt|ago }}</span>
        <h3 class="timeline-header no-border"><a href="#">{{ stream.user is not null ? stream.user.firstname ~ " " ~ stream.user.lastname  : "" }}</a> added project "{{ entity.name }}"</h3>
    </div>
</li>